# Changelog

## 0.1.0

- Estructura inicial del proyecto Honey.
- Instalador local con `install`, `uninstall`, `doctor` y `paths`.
- CLI `honey` con `run`, `new`, `doctor`, `version` y `help`.
- Lenguaje minimo con `print`, `env`, `sleep` y `exit`.
- Documentacion inicial extensa.

